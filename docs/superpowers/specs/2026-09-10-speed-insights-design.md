# Speed Insights optimization

User approved the complete scope with `confirm all`: investigate route/device/deployment metrics, JavaScript, CSS/fonts, server waiting, images, layout stability, and regression verification. No database operations.

The observed seven-day desktop RES is 55, with dashboard and items scoring 55, login 98 and DB panel 100. Only 42 events were available; detailed metrics require Plus. These observations do not establish a specific metric or current-commit cause.

Implement the confirmed unnecessary costs first: remove the global external icon font used by one settings error icon; load the global creation sheet only on demand. Preserve URL-triggered opening, existing dirty-form confirmation, close/reopen behavior and authorization. Keep the loaded sheet mounted after first use. Show loading feedback and allow retry after import failure.

Review data request ordering/cache, image sizing/loading and skeleton geometry. Change these only when evidence identifies a defect; do not speculate about RES gains or weaken authentication. Compare production build assets under the same toolchain and run provider regressions, typecheck, lint, tests and build. Real-user score validation requires fresh post-deployment traffic; report that separately from build results.

Self-review: bounded scope, no new dependencies, no database or authorization changes, no score guarantee. The written scope mirrors the user's approved plan.
